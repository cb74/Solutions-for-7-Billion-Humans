-- 7 Billion Humans --
-- 40: Consignes d'Impression 2 --

a:
mem1 = nearest printer
mem2 = calc mem2 + 1
if mem2 < 6:
	takefrom mem1
	write mem2
	b:
	if c != nothing:
		step nw,w,sw,n,s,ne,e,se
		jump b
	endif
	drop
	jump a
endif
